# Convert p-values into significance stars

Convert p-values into significance stars

## Usage

``` r
make_stars(pval)
```

## Arguments

- pval:

  Numeric vector of p-values.

## Value

Character vector of the same length as `pval`, containing `"***"`,
`"**"`, `"*"`, `"."`, or `""`.
