# Print method for constructtariffclasses objects

Displays the tariff class splits of an object created by
[`construct_tariff_classes()`](https://mharinga.github.io/insurancerating/reference/construct_tariff_classes.md).

## Usage

``` r
# S3 method for class 'constructtariffclasses'
print(x, ...)
```

## Arguments

- x:

  An object of class `"constructtariffclasses"`.

- ...:

  Further arguments passed to or from other methods (ignored).

## Value

Invisibly returns `x`.
