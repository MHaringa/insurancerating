% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_reduction_db.R
\name{merge_date_ranges_db}
\alias{merge_date_ranges_db}
\title{Merge connected portfolio periods in DuckDB}
\usage{
merge_date_ranges_db(
  data,
  period_start,
  period_end,
  group_by,
  aggregate_cols = NULL,
  aggregate_fun = c("sum", "mean", "min", "max"),
  merge_gap_days = 1
)
}
\arguments{
\item{data}{A lazy DuckDB table created with \code{\link[dplyr:tbl]{dplyr::tbl()}}.}

\item{period_start}{Character string naming the period-start column.}

\item{period_end}{Character string naming the period-end column.}

\item{group_by}{Character vector identifying the policy, risk or segment
within which periods may be combined.}

\item{aggregate_cols}{Optional character vector naming numeric columns to
aggregate over each merged period.}

\item{aggregate_fun}{Character string specifying the SQL aggregation for
\code{aggregate_cols}: \code{"sum"}, \code{"mean"}, \code{"min"}, or \code{"max"}.}

\item{merge_gap_days}{Non-negative whole number. The interpretation is the
same as in \code{\link[=merge_date_ranges]{merge_date_ranges()}}: \code{1} merges overlapping and directly
adjacent periods, while \code{0} merges overlapping periods only.}
}
\value{
A lazy DuckDB table. Use \code{\link[dbplyr:sql_render]{dbplyr::sql_render()}} to inspect the SQL or
\code{\link[dplyr:collect]{dplyr::collect()}} to import the consolidated periods into R.
}
\description{
Construct the temporal portfolio reduction performed by
\code{\link[=merge_date_ranges]{merge_date_ranges()}} as a lazy DuckDB query. Overlapping or adjacent periods
are merged inside DuckDB, so only the consolidated periods need to be copied
into R.
}
\details{
Merging connected intervals is a SQL gaps-and-islands calculation. The query
uses ordered window functions to compare each start date with the latest
preceding end date, assigns an interval identifier, and then aggregates each
resulting interval.

Date arithmetic differs between database systems. This implementation is
therefore deliberately restricted to DuckDB. It does not download the
source table and does not create a permanent database table.
}
\examples{
\dontrun{
con <- DBI::dbConnect(duckdb::duckdb())
portfolio_db <- dplyr::tbl(con, "portfolio_periods")

periods_db <- merge_date_ranges_db(
  portfolio_db,
  period_start = "period_start",
  period_end = "period_end",
  group_by = c("policy_id", "coverage"),
  aggregate_cols = c("earned_exposure", "earned_premium")
)

periods <- dplyr::collect(periods_db)
DBI::dbDisconnect(con, shutdown = TRUE)
}

}
\seealso{
\code{\link[=merge_date_ranges]{merge_date_ranges()}}, \code{\link[=rating_grid_db]{rating_grid_db()}}, \code{\link[=rating_grid]{rating_grid()}}
}
\author{
Martin Haringa
}
