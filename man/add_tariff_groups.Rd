% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam_construct_tariff_classes.R
\name{add_tariff_groups}
\alias{add_tariff_groups}
\title{Add derived tariff groups to portfolio data}
\usage{
add_tariff_groups(data, groups, name = NULL, overwrite = FALSE)
}
\arguments{
\item{data}{A data frame to which the tariff groups should be added.}

\item{groups}{Object of class \code{"tariff_groups"}, produced by
\code{\link[=derive_tariff_groups]{derive_tariff_groups()}}. Old \code{"tariff_classes"} objects are accepted for
backward compatibility.}

\item{name}{Character string. Name of the new output column. If \code{NULL}, the
name is based on the risk factor name, for example
\code{"age_policyholder_group"}.}

\item{overwrite}{Logical. If \code{FALSE}, the function stops when \code{name} already
exists in \code{data}.}
}
\value{
A data frame with the derived tariff group column added.
}
\description{
Adds the tariff groups derived by \code{\link[=derive_tariff_groups]{derive_tariff_groups()}} as a new factor
column to a portfolio data set. This is the recommended way to attach derived
tariff groups to the same portfolio rows that were used to fit the risk
factor GAM.
}
\examples{
\dontrun{
age_groups <- risk_factor_gam(
  MTPL,
  risk_factor = "age_policyholder",
  claim_count = "nclaims",
  exposure = "exposure"
) |>
  derive_tariff_groups()

MTPL |>
  add_tariff_groups(age_groups, name = "age_policyholder_group")
}

}
\author{
Martin Haringa
}
