% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity_distribution.R
\name{plot_severity_distribution}
\alias{plot_severity_distribution}
\title{Exploratory severity diagnostics by category}
\usage{
plot_severity_distribution(
  data,
  claim_amount,
  risk_factor = NULL,
  top_n = 10,
  min_claims = 20,
  sort = c("median", "mean", "n_claims"),
  threshold = NULL,
  mean = TRUE,
  median = TRUE,
  distribution = c("none", "half_violin", "violin"),
  point_method = c("quasirandom", "jitter", "none"),
  orientation = c("horizontal", "vertical"),
  log_scale = TRUE,
  boxplot = FALSE,
  boxplot_width = 0.06,
  show_labels = TRUE,
  all_claims_label = "All claims",
  mean_label = "Mean",
  median_label = "Median",
  threshold_label = "Threshold",
  x_label = NULL,
  y_label = NULL,
  point_alpha = 0.16,
  point_size = 0.75,
  point_width = 0.15
)
}
\arguments{
\item{data}{A \code{data.frame} with claim-level observations.}

\item{claim_amount}{Character string. Name of the claim amount column.}

\item{risk_factor}{Optional character string. Name of the risk factor used
to split the severity distribution. If \code{NULL}, the overall claim amount
distribution is shown.}

\item{top_n}{Positive whole number. Number of categories to keep after
filtering and sorting.}

\item{min_claims}{Positive whole number. Categories with fewer than this
number of claim observations are removed.}

\item{sort}{Character. Metric used to sort and select categories. One of
\code{"median"}, \code{"mean"} or \code{"n_claims"}.}

\item{threshold}{Optional numeric scalar. If supplied, claims above this
threshold are highlighted and a dotted threshold line is shown.}

\item{mean}{Logical. If \code{TRUE}, add a marker for the average claim amount.}

\item{median}{Logical. If \code{TRUE}, add a marker for the median claim amount.}

\item{distribution}{Character. Distribution layer. One of \code{"none"},
\code{"half_violin"} or \code{"violin"}. Default is \code{"none"}.}

\item{point_method}{Character. Point placement method. One of
\code{"quasirandom"}, \code{"jitter"} or \code{"none"}.}

\item{orientation}{Character. \code{"horizontal"} places claim amount on the
x-axis and categories on the y-axis. \code{"vertical"} reverses this.}

\item{log_scale}{Logical. If \code{TRUE}, use a log10 scale for claim amounts.}

\item{boxplot}{Logical. If \code{TRUE}, add a small centred boxplot. Default is
\code{FALSE}.}

\item{boxplot_width}{Numeric scalar. Width of the optional boxplot. Smaller
values keep the boxplot as a subtle summary layer behind the individual
claim points.}

\item{show_labels}{Logical. If \code{TRUE}, add direct labels for the mean,
median and, when supplied, threshold. Requires the suggested package
\code{ggrepel}.}

\item{all_claims_label}{Character string used as the category label when
\code{risk_factor = NULL}.}

\item{mean_label}{Character string used for the direct mean marker label.
Default is \code{"Mean"}.}

\item{median_label}{Character string used for the direct median marker label.
Default is \code{"Median"}.}

\item{threshold_label}{Character string used for the optional threshold
label.}

\item{x_label}{Optional character string. X-axis label. If \code{NULL}, a default is
chosen from \code{claim_amount}, \code{risk_factor} and \code{orientation}.}

\item{y_label}{Optional character string. Y-axis label. If \code{NULL}, a default is
chosen from \code{claim_amount}, \code{risk_factor} and \code{orientation}.}

\item{point_alpha}{Numeric alpha for raw claim points.}

\item{point_size}{Numeric point size for raw claim points.}

\item{point_width}{Numeric spread for raw claim points.}
}
\value{
A ggplot object. The plot can be extended with regular ggplot2
syntax, for example \code{+ ggplot2::labs(caption = "...")} or
\code{+ ggplot2::theme(...)}.
}
\description{
Visualise individual claim amounts overall or per risk factor.

Average claim amounts can be misleading because a small number of large
losses may dominate the mean. \code{plot_severity_distribution()} shows the full
claim amount distribution, usually on a log scale, together with mean and
median claim amount markers. If \code{risk_factor} is supplied, the distribution
is shown per level of that risk factor. If \code{risk_factor = NULL}, the function
shows the overall claim amount distribution. This makes heavy tails, clusters
of small claims, spread differences, extreme losses and distributional shape
visible in a way that average severity alone cannot.

The function is intended for exploratory severity diagnostics in pricing
analysis, portfolio diagnostics, tariff notes, exploratory segmentation
analysis and severity model validation. It uses standard evaluation: pass
column names as character strings through \code{claim_amount} and \code{risk_factor}.

If \code{threshold} is supplied, claims above the threshold are highlighted in
\code{"firebrick"} and a dotted threshold line is added. Claims at or below the
threshold remain light grey. Direct labels for the mean, median and optional
threshold are added with \code{ggrepel} when \code{show_labels = TRUE}; \code{ggrepel} is a
suggested package and is not imported as a hard dependency.
}
\examples{
x <- plot_severity_distribution(
  MTPL,
  claim_amount = "amount",
  risk_factor = "zip",
  top_n = 4,
  min_claims = 20,
  point_method = "jitter",
  show_labels = FALSE
)
print(x)

x_threshold <- plot_severity_distribution(
  MTPL,
  claim_amount = "amount",
  risk_factor = NULL,
  threshold = 10000,
  min_claims = 20,
  point_method = "jitter",
  show_labels = FALSE
)

}
\author{
Martin Haringa
}
