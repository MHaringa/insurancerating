% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excess_loss.R
\name{add_excess_loading}
\alias{add_excess_loading}
\title{Add excess loading to a pricing portfolio}
\usage{
add_excess_loading(
  data,
  allocation,
  base_premium = "base_premium",
  allocated_excess_loss = NULL,
  allocated_loading = NULL,
  weight = NULL,
  output = c("premium", "rate")
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{allocation}{An object returned by \code{\link[=allocate_excess_loss]{allocate_excess_loss()}}.}

\item{base_premium}{Character string. Base premium amount before excess
loading.}

\item{allocated_excess_loss}{Optional character string. Column in
\code{allocation$data} with the allocated excess burden in absolute monetary
terms. If \code{NULL}, \code{allocated_excess_loss} is used.}

\item{allocated_loading}{Optional character string. Column in
\code{allocation$data} with the excess loading per unit of \code{weight}. If \code{NULL},
\code{allocated_loading} is used.}

\item{weight}{Optional character string. Weight column used to convert between
premium amounts and rates when \code{output = "rate"}.}

\item{output}{Character. Use \code{"premium"} to return premium amounts or
\code{"rate"} to return rates per unit of \code{weight}.}
}
\value{
A \code{data.frame}. With \code{output = "premium"}, the result contains
\code{base_premium}, \code{allocated_excess_loss}, \code{allocated_loading},
\code{excess_loading} and \code{loaded_premium}. With \code{output = "rate"}, it contains
\code{base_rate}, \code{allocated_loading} and \code{loaded_rate}.
}
\description{
Add an allocated excess loading to a portfolio data set.

\code{add_excess_loading()} is the final workflow step. It does not estimate or
allocate excess loss. It takes an object produced by \code{\link[=allocate_excess_loss]{allocate_excess_loss()}}
and adds row-level loading columns to \code{data}.

The default workflow is premium based:
\code{loaded_premium = base_premium + allocated_excess_loss}.
\code{allocated_excess_loss} represents the allocated excess burden in absolute
monetary terms. \code{allocated_loading} represents the excess loading per unit of
weight.

Use \code{output = "rate"} when \code{base_premium} should be interpreted as a premium
amount that first needs to be converted to a rate with
\code{base_rate = base_premium / weight}.
}
\examples{
claims <- data.frame(
  sector = rep(c("Industry", "Retail"), each = 4),
  claim_amount = c(1000, 120000, 30000, 8000, 2000, 150000, 40000, 6000),
  earned_exposure = rep(1, 8)
)
decomposed <- calculate_excess_loss(claims, "claim_amount", threshold = 100000)
allocation <- allocate_excess_loss(
  decomposed,
  excess_amount = "excess_claim_amount",
  weight = "earned_exposure"
)
add_excess_loading(decomposed, allocation)

}
\author{
Martin Haringa
}
