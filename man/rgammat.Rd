% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truncated_distributions.R
\name{rgammat}
\alias{rgammat}
\title{Simulate severities from a truncated gamma distribution}
\usage{
rgammat(n, shape, scale, lower, upper)
}
\arguments{
\item{n}{Integer. Number of observations to generate.}

\item{shape}{Numeric. Shape parameter of the gamma distribution.}

\item{scale}{Numeric. Scale parameter of the gamma distribution.}

\item{lower}{Numeric. Lower truncation bound.}

\item{upper}{Numeric. Upper truncation bound.}
}
\value{
A numeric vector of length \code{n} containing random draws from the
truncated gamma distribution.
}
\description{
Generate random claim severities from a gamma distribution conditional on
the result falling inside the interval \eqn{(lower, upper)}.
}
\details{
Random values are generated by sampling from a uniform distribution on the
interval \eqn{[F(lower), F(upper)]}, where \eqn{F} is the CDF of the gamma
distribution, and then applying the inverse CDF.

The resulting sample follows the specified conditional distribution; values
outside the truncation interval are not generated.

In severity analysis, this can be used for simulation and model checking when
the available claims are observed only between a lower reporting threshold
and an upper modelling limit. Truncation should not be confused with
censoring or capping: the function assumes that values outside the interval
are absent rather than recorded at a boundary.
}
\seealso{
\code{\link[=fit_truncated_severity]{fit_truncated_severity()}}, \code{\link[=rlnormt]{rlnormt()}}
}
\author{
Martin Haringa
}
