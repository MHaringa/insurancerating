% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_refinement.R
\name{split_level}
\alias{split_level}
\title{Define a sublevel split for a model level}
\usage{
split_level(level, new_levels, relativities)
}
\arguments{
\item{level}{Character string. Existing level of the risk factor to split.}

\item{new_levels}{Character vector. Levels of the more detailed portfolio
variable within \code{level}.}

\item{relativities}{Numeric vector. Multiplicative relativities corresponding
to \code{new_levels}. Must have the same length as \code{new_levels}.}
}
\value{
A named list of length one. Its name is \code{level}; its value is a data
frame with columns \code{new_level} and \code{relativity}.
}
\description{
Define how one level of a GLM risk factor is divided into more detailed
portfolio levels with specified multiplicative relativities. The resulting
object is intended to be combined with \code{\link[=relativities]{relativities()}} and supplied to
\code{\link[=add_relativities]{add_relativities()}}.
}
\details{
\code{level} identifies the existing parent level in \code{model_variable}.
\code{new_levels} identifies the corresponding levels of \code{split_variable}.
\code{relativities} gives their relative tariff effects before any optional
exposure normalisation by \code{\link[=add_relativities]{add_relativities()}}.

This helper defines a tariff assumption; it does not estimate relativities
from claim experience and does not alter a fitted GLM.
}
\examples{
split_level(
  level = "construction",
  new_levels = c("residential", "commercial", "civil"),
  relativities = c(1.00, 1.10, 1.25)
)

}
\seealso{
\code{\link[=relativities]{relativities()}}, \code{\link[=add_relativities]{add_relativities()}}
}
\author{
Martin Haringa
}
