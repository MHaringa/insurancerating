% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excess_loss.R
\name{as_gt}
\alias{as_gt}
\alias{as_gt.threshold_assessment}
\title{Convert an object to a gt table}
\usage{
as_gt(x, ...)

\method{as_gt}{threshold_assessment}(
  x,
  claims = TRUE,
  loss = FALSE,
  premium = TRUE,
  locale = "nl-NL",
  loss_decimals = 0,
  premium_decimals = 0,
  ratio_decimals = 1,
  color_last_column = TRUE,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=assess_excess_threshold]{assess_excess_threshold()}}.}

\item{...}{Unused.}

\item{claims}{Logical. If \code{TRUE}, include claim-count columns.}

\item{loss}{Logical. If \code{TRUE}, include loss amount columns. The default is
\code{FALSE} to keep the threshold comparison compact.}

\item{premium}{Logical. If \code{TRUE}, include pure-premium and premium-reduction
columns.}

\item{locale}{Character. Locale used for number formatting, for example
\code{"nl-NL"} or \code{"en-US"}.}

\item{loss_decimals, premium_decimals, ratio_decimals}{Non-negative whole
numbers controlling displayed decimals for loss amounts, premium amounts
and percentage ratios.}

\item{color_last_column}{Logical. If \code{TRUE}, color the final displayed
column from white to yellow so the highest values stand out in the
presentation table.}

\item{title}{Optional character. Table title. If \code{NULL}, no table title is
added.}

\item{subtitle}{Optional character. Table subtitle. If \code{NULL}, no table
subtitle is added.}
}
\value{
A \code{gt_tbl} object for supported methods.

A \code{gt_tbl} object.
}
\description{
Generic presentation helper. Methods return a \code{gt} table for objects where a
formatted reporting table is more useful than another plot.

Create a formatted \code{gt} table from an object returned by
\code{\link[=assess_excess_threshold]{assess_excess_threshold()}}. The original object remains a regular
\code{data.frame} subclass; \code{as_gt()} is only used when a presentation table is
needed for a report, tariff note or pricing review.
}
\examples{
portfolio <- data.frame(
  policy_id = 1:10,
  sector = rep(c("Industry", "Retail"), each = 5),
  claim_count = c(
    0, 1, 1, 1, 1,
    0, 1, 1, 1, 1
  ),
  claim_amount = c(
    0, 25000, 120000, 50000, 175000,
    0, 40000, 90000, 150000, 300000
  ),
  policy_years = rep(1, 10)
)

thresholds <- assess_excess_threshold(
  data = portfolio,
  claim_amount = "claim_amount",
  thresholds = c(25000, 50000, 100000, 150000),
  exposure = "policy_years",
  group = "sector",
  claim_count = "claim_count"
)

if (requireNamespace("gt", quietly = TRUE)) {
  as_gt(thresholds)
}

}
\author{
Martin Haringa
}
