% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excess_loss.R, R/model_rating_table_gt.R
\name{as_gt}
\alias{as_gt}
\alias{as_gt.threshold_assessment}
\alias{as_gt.rating_table}
\title{Convert an object to a gt table}
\usage{
as_gt(x, ...)

\method{as_gt}{threshold_assessment}(
  x,
  claims = TRUE,
  loss = FALSE,
  premium = TRUE,
  locale = "nl-NL",
  loss_decimals = 0,
  premium_decimals = 0,
  ratio_decimals = 1,
  color_last_column = TRUE,
  title = NULL,
  subtitle = NULL,
  ...
)

\method{as_gt}{rating_table}(
  x,
  significance = NULL,
  locale = "nl-NL",
  estimate_decimals = 3,
  exposure_decimals = 0,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{x}{A supported object to convert, such as a \code{threshold_assessment}
returned by \code{\link[=assess_excess_threshold]{assess_excess_threshold()}} or a \code{rating_table} returned by
\code{\link[=rating_table]{rating_table()}}.}

\item{...}{Arguments passed to methods.}

\item{claims}{Logical. If \code{TRUE}, include claim-count columns.}

\item{loss}{Logical. If \code{TRUE}, include loss amount columns. The default is
\code{FALSE} to keep the threshold comparison compact.}

\item{premium}{Logical. If \code{TRUE}, include pure-premium and premium-reduction
columns.}

\item{locale}{Character. Locale used to format model effects and exposure,
for example \code{"nl-NL"} or \code{"en-US"}.}

\item{loss_decimals, premium_decimals, ratio_decimals}{Non-negative whole
numbers controlling displayed decimals for loss amounts, premium amounts
and percentage ratios.}

\item{color_last_column}{Logical. If \code{TRUE}, color the final displayed
column from white to yellow so the highest values stand out in the
presentation table.}

\item{title}{Optional character. Table title. If \code{NULL}, no title is added.}

\item{subtitle}{Optional character. Table subtitle. If \code{NULL}, no subtitle is
added.}

\item{significance}{Optional logical. If \code{NULL}, use the significance setting
stored on \code{x}. If \code{TRUE}, append the stored significance stars to the model
effects and add the significance-level note. If \code{FALSE}, show fitted
effects without stars.}

\item{estimate_decimals}{Non-negative whole number. Number of decimals shown
for fitted coefficients or relativities.}

\item{exposure_decimals}{Non-negative whole number. Number of decimals shown
for the exposure column, when available.}
}
\value{
A \code{gt_tbl} object for supported methods.
}
\description{
Generic presentation helper. Methods return a \code{gt} table for objects where a
formatted reporting table is more useful than another plot.

Create a formatted \code{gt} table from an object returned by
\code{\link[=assess_excess_threshold]{assess_excess_threshold()}}. The original object remains a regular
\code{data.frame} subclass; \code{as_gt()} is only used when a presentation table is
needed for a report, tariff note or pricing review.

Create a formatted \code{gt} table from an object returned by \code{\link[=rating_table]{rating_table()}}.
Risk factors are presented as row groups, while fitted model effects are
shown as relativities or coefficients depending on the scale selected in
\code{\link[=rating_table]{rating_table()}}.
}
\details{
The first column of a \code{rating_table} identifies the model risk factor.
\code{as_gt()} uses this column as \code{groupname_col} and sets
\code{row_group_as_column = TRUE}. Levels belonging to the same risk factor are
therefore kept together in a compact format suitable for a tariff note,
model review or technical appendix.

With \code{significance = TRUE}, significance stars are appended to the fitted
effects and the significance levels are shown below the table. This requires
an object originally created with \code{rating_table(significance = TRUE)},
because p-value information is deliberately not retained when significance
is disabled during table construction. Significance stars are a statistical
diagnostic and should be interpreted together with exposure, effect size,
model stability and actuarial relevance.
}
\examples{
portfolio <- data.frame(
  policy_id = 1:10,
  sector = rep(c("Industry", "Retail"), each = 5),
  claim_count = c(
    0, 1, 1, 1, 1,
    0, 1, 1, 1, 1
  ),
  claim_amount = c(
    0, 25000, 120000, 50000, 175000,
    0, 40000, 90000, 150000, 300000
  ),
  policy_years = rep(1, 10)
)

thresholds <- assess_excess_threshold(
  data = portfolio,
  claim_amount = "claim_amount",
  thresholds = c(25000, 50000, 100000, 150000),
  exposure = "policy_years",
  group = "sector",
  claim_count = "claim_count"
)

if (requireNamespace("gt", quietly = TRUE)) {
  as_gt(thresholds)
}

portfolio <- MTPL
portfolio$zip <- as.factor(portfolio$zip)

frequency_model <- glm(
  nclaims ~ bm + zip + offset(log(exposure)),
  family = poisson(),
  data = portfolio
)

fitted_tariff <- rating_table(
  frequency_model,
  model_data = portfolio,
  exposure = "exposure",
  significance = TRUE
)

if (requireNamespace("gt", quietly = TRUE)) {
  as_gt(fitted_tariff)
  as_gt(fitted_tariff, significance = FALSE, locale = "en-US")
}

}
\author{
Martin Haringa
}
