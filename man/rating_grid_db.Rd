% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_reduction_db.R
\name{rating_grid_db}
\alias{rating_grid_db}
\title{Reduce a database portfolio to observed rating-grid points}
\usage{
rating_grid_db(
  x,
  group_by,
  exposure = NULL,
  aggregate_cols = NULL,
  drop_na = FALSE,
  exposure_by = NULL
)
}
\arguments{
\item{x}{A lazy database table created with \code{\link[dplyr:tbl]{dplyr::tbl()}}.}

\item{group_by}{Character vector containing the columns that define an
observed rating-grid point.}

\item{exposure}{Optional character string naming an exposure column to sum.
If \code{NULL}, the output contains a \code{count} column with the number of source
records in each combination.}

\item{aggregate_cols}{Optional character vector naming additional columns to
sum within each combination.}

\item{drop_na}{Logical. If \code{TRUE}, records with missing \code{group_by} values are
excluded. If \code{FALSE}, missing values remain an observed group.}

\item{exposure_by}{Reserved for consistency with \code{\link[=rating_grid]{rating_grid()}}. Splitting
an exposure into dynamically named wide columns is not performed in a lazy
database query. Include this variable in \code{group_by}, collect the reduced
long table, and reshape it in R instead.}
}
\value{
A lazy database table. Use \code{\link[dbplyr:sql_render]{dbplyr::sql_render()}} to inspect the SQL
or \code{\link[dplyr:collect]{dplyr::collect()}} to import the reduced result into R.
}
\description{
Construct the same type of observed rating-factor combinations as
\code{\link[=rating_grid]{rating_grid()}}, while leaving the calculation in the database. The function
returns a lazy query and does not copy the source portfolio into R.
Each output row is a model point: one observed covariate combination together
with aggregated exposure and other additive quantities requested by the user.

This is useful when the row-level portfolio is too large for available R
memory. The database performs the grouping and aggregation; the reduced
result can subsequently be imported with \code{\link[dplyr:collect]{dplyr::collect()}}.
}
\details{
\code{rating_grid_db()} performs only operations that translate naturally to SQL:
grouping, row counting and sums. Column names are supplied as strings. No SQL
is executed until the lazy query is printed, collected or otherwise used by
the database backend.

Database tables have no inherent row order. Apply \code{\link[dplyr:arrange]{dplyr::arrange()}} after
the final database operation when a specific presentation order is required.

The database table should already contain the rating variables required for
the intended model or portfolio analysis. Unlike \code{\link[=rating_grid]{rating_grid()}}, this
function does not inspect a fitted R model or refinement metadata because
those objects are held in R rather than in the database.
}
\examples{
\dontrun{
con <- DBI::dbConnect(duckdb::duckdb())
portfolio_db <- dplyr::tbl(con, "portfolio")

grid_db <- rating_grid_db(
  portfolio_db,
  group_by = c("sector", "region"),
  exposure = "earned_exposure",
  aggregate_cols = "earned_premium"
)

dbplyr::sql_render(grid_db)
grid <- dplyr::collect(grid_db)
DBI::dbDisconnect(con, shutdown = TRUE)
}

}
\seealso{
\code{\link[=rating_grid]{rating_grid()}}, \code{\link[=merge_date_ranges_db]{merge_date_ranges_db()}}, \code{\link[=merge_date_ranges]{merge_date_ranges()}}
}
\author{
Martin Haringa
}
