% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_calibration.R
\name{calibrate_model}
\alias{calibrate_model}
\title{Calibrate the overall level of a refined pricing model}
\usage{
calibrate_model(model, factor)
}
\arguments{
\item{model}{A fitted refined GLM returned by \code{\link[=refit]{refit()}}. It must inherit from
\code{refitrestricted} or \code{refitsmooth} and use a log link.}

\item{factor}{Positive finite numeric scalar. \code{1} retains the prediction
level, values above 1 increase it, and values below 1 decrease it.}
}
\value{
A fitted \code{glm} that also inherits from \code{calibrated_model}. Attributes
\code{calibration_factor}, \code{calibration_original_intercept},
\code{calibration_intercept}, \code{calibration_log_shift}, \code{calibration_call} and
\code{calibrated_at} record the calibration.
}
\description{
Adjust the overall prediction level of a fitted model returned by \code{\link[=refit]{refit()}}
without re-estimating its relative tariff structure. Calibration is a final
model-level operation: all refinement decisions must be completed before
calling \code{calibrate_model()}.
}
\details{
For a refined GLM with a log link, calibration adds \code{log(factor)} to the
intercept. Consequently, every response-scale prediction is multiplied by
\code{factor}, while all non-intercept coefficients and tariff relativities remain
unchanged.

The returned object is a copied, internally consistent fitted model. Its
coefficients, linear predictors, fitted values, working residuals, deviance
and AIC are updated to the calibrated level. The original refined model is
not modified. Calibration metadata store the factor, log shift, original and
calibrated intercept, creation time and call.
\subsection{Refinement and calibration}{

Model refinement changes or constrains the relative tariff structure and is
evaluated through \code{prepare_refinement()}, one or more \verb{add_*()} operations,
and \code{\link[=refit]{refit()}}. Model calibration changes only the final overall level. A
calibrated model cannot be calibrated again or used as the starting point
for further refinement. Retain the \code{rating_refinement} specification and
recalibrate a newly refitted model if earlier decisions need to be revised.
}
}
\examples{
restrictions <- data.frame(
  zip = c(0, 1, 2, 3),
  zip_restricted = c(0.90, 1.00, 1.05, 1.10)
)

mod_initial <- glm(
  nclaims ~ zip + offset(log(exposure)),
  family = poisson(),
  data = MTPL
)

mod_refined <- mod_initial |>
  prepare_refinement() |>
  add_restriction(restrictions) |>
  refit(intercept_only = TRUE)

mod_calibrated <- calibrate_model(mod_refined, factor = 1.05)

rating_table(mod_calibrated)

data_final <- mod_refined$data |>
  add_prediction(
    mod_calibrated,
    predictions = "net_risk_premium"
  )

}
\seealso{
\code{\link[=refit]{refit()}}, \code{\link[=rating_table]{rating_table()}}, \code{\link[=add_prediction]{add_prediction()}},
\code{\link[=audit_refinement]{audit_refinement()}}
}
