% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rating_table.R
\name{add_observed_experience}
\alias{add_observed_experience}
\title{Add observed portfolio experience to a rating table}
\usage{
add_observed_experience(
  object,
  experience,
  metric = "risk_premium",
  label = "Observed experience",
  color = NULL,
  scale = c("reference", "mean")
)
}
\arguments{
\item{object}{A \code{rating_table} object returned by \code{\link[=rating_table]{rating_table()}}.}

\item{experience}{A \code{factor_analysis} object returned by \code{\link[=factor_analysis]{factor_analysis()}}.}

\item{metric}{Character; metric from \code{experience} to plot. Common choices are
\code{"frequency"}, \code{"average_severity"}, \code{"risk_premium"}, \code{"loss_ratio"} and
\code{"average_premium"}, depending on which columns were supplied to
\code{\link[=factor_analysis]{factor_analysis()}}.}

\item{label}{Character; legend label for the observed experience line.}

\item{color}{Optional line color. If \code{NULL}, the internal risk premium color
is used.}

\item{scale}{Character; scaling applied before plotting. One of
\code{"reference"} or \code{"mean"}.}
}
\value{
A \code{rating_table} object with observed portfolio experience attached.
}
\description{
Attach the output of \code{\link[=factor_analysis]{factor_analysis()}} to a \code{\link[=rating_table]{rating_table()}} object so it
can be shown in \code{\link[=autoplot.rating_table]{autoplot.rating_table()}}. This is useful when you want to
compare fitted GLM relativities with the observed portfolio pattern for the
same rating factor.

The observed metric is scaled before plotting. With \code{scale = "reference"}
the metric is divided by the observed value of the model reference level. If
a clear reference level cannot be found, the metric is scaled to its mean.
With \code{scale = "mean"}, the metric is always scaled to its mean.
}
\examples{
df <- MTPL2
df$area <- as.factor(df$area)

model <- glm(
  nclaims ~ area + offset(log(exposure)),
  family = poisson(),
  data = df
)

observed <- factor_analysis(
  df,
  risk_factors = "area",
  claim_count = "nclaims",
  exposure = "exposure"
)

rating_table(model, model_data = df, exposure = "exposure") |>
  add_observed_experience(observed, metric = "frequency") |>
  autoplot(risk_factors = "area")

}
\author{
Martin Haringa
}
