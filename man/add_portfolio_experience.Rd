% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rating_table.R
\name{add_portfolio_experience}
\alias{add_portfolio_experience}
\alias{add_portfolio_experience.rating_table}
\title{Add portfolio experience to a rating table}
\usage{
add_portfolio_experience(x, ...)

\method{add_portfolio_experience}{rating_table}(
  x,
  observed = NULL,
  data = NULL,
  risk_factors = NULL,
  claim_count = NULL,
  exposure = NULL,
  claim_amount = NULL,
  metric = NULL,
  label = "Observed experience",
  color = NULL,
  scale = c("reference", "mean"),
  experience = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{rating_table} object returned by \code{\link[=rating_table]{rating_table()}}.}

\item{...}{Unused.}

\item{observed}{Optional \code{\link[=factor_analysis]{factor_analysis()}} object or list of
\code{\link[=factor_analysis]{factor_analysis()}} objects. If supplied, these observed summaries are
attached directly.}

\item{data}{Optional \code{data.frame}. If \code{observed = NULL}, observed experience
is calculated from this data.}

\item{risk_factors}{Optional character vector. Risk factors for which
observed experience should be calculated. If \code{NULL}, all risk factors in
the rating table are used.}

\item{claim_count}{Optional character string. Claim count column used by
\code{\link[=factor_analysis]{factor_analysis()}}.}

\item{exposure}{Optional character string. Exposure column used by
\code{\link[=factor_analysis]{factor_analysis()}}.}

\item{claim_amount}{Optional character string. Claim amount column used by
\code{\link[=factor_analysis]{factor_analysis()}}.}

\item{metric}{Optional character string. Default observed metric to plot.
Common choices are \code{"frequency"}, \code{"severity"}/\code{"average_severity"} and
\code{"risk_premium"}. The metric can also be overridden in
\code{\link[=autoplot.rating_table]{autoplot.rating_table()}}.}

\item{label}{Character; legend label for the observed experience line.}

\item{color}{Optional line color. If \code{NULL}, the internal risk premium color
is used.}

\item{scale}{Character; scaling applied before plotting. One of
\code{"reference"} or \code{"mean"}.}

\item{experience}{Deprecated alias for \code{observed}.}
}
\value{
A \code{rating_table} object with observed portfolio experience attached.
}
\description{
\code{add_portfolio_experience()} enriches a \code{\link[=rating_table]{rating_table()}} object with observed
portfolio experience. When \code{data} is supplied, observed experience is
calculated automatically for all risk factors in the rating table, unless
\code{risk_factors} is specified. Existing \code{\link[=factor_analysis]{factor_analysis()}} results can also be
supplied through \code{observed}.

This makes it possible to compare fitted GLM relativities with observed
portfolio patterns in \code{\link[=autoplot.rating_table]{autoplot.rating_table()}}. The full observed output is
stored on the rating table, so \code{\link[=autoplot.rating_table]{autoplot.rating_table()}} can later switch
between metrics such as \code{"frequency"}, \code{"average_severity"} and
\code{"risk_premium"} without recalculating the summaries.

The observed metric is scaled before plotting. With \code{scale = "reference"}
the metric is divided by the observed value of the model reference level. If
a clear reference level cannot be found, the metric is scaled to its mean.
With \code{scale = "mean"}, the metric is always scaled to its mean.
}
\examples{
df <- MTPL2
df$area <- as.factor(df$area)

model <- glm(
  nclaims ~ area + offset(log(exposure)),
  family = poisson(),
  data = df
)

rating_table(model, model_data = df, exposure = "exposure") |>
  add_portfolio_experience(
    data = df,
    claim_count = "nclaims",
    exposure = "exposure"
  ) |>
  autoplot(risk_factors = "area", metric = "frequency")

observed <- factor_analysis(
  df,
  risk_factors = "area",
  claim_count = "nclaims",
  exposure = "exposure"
)

rating_table(model, model_data = df, exposure = "exposure") |>
  add_portfolio_experience(observed = observed) |>
  autoplot(risk_factors = "area")

}
\author{
Martin Haringa
}
