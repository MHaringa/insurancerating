% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam_construct_tariff_classes.R
\name{add_tariff_segments}
\alias{add_tariff_segments}
\title{Add derived tariff segments to portfolio data}
\usage{
add_tariff_segments(data, segments, name = NULL, overwrite = FALSE)
}
\arguments{
\item{data}{A data frame to which the tariff segments should be added.}

\item{segments}{Object of class \code{"tariff_segments"}, produced by
\code{\link[=derive_tariff_segments]{derive_tariff_segments()}}. Old \code{"tariff_classes"} objects are accepted for
backward compatibility.}

\item{name}{Character string. Name of the new output column. If \code{NULL}, the
name is based on the risk factor name, for example
\code{"age_policyholder_segment"}.}

\item{overwrite}{Logical. If \code{FALSE}, the function stops when \code{name} already
exists in \code{data}.}
}
\value{
A data frame with the derived tariff segment column added.
}
\description{
Adds the tariff segments derived by \code{\link[=derive_tariff_segments]{derive_tariff_segments()}} as a new factor
column to a portfolio data set. The stored boundaries are applied to the
original continuous risk-factor column, so the result does not depend on the
row order used when the GAM was fitted.

The helper does not re-estimate the GAM or derive new boundaries. It can be
used after filtering or reordering the original portfolio and on new data
whose risk-factor values remain within the range used to derive the
segmentation.
}
\details{
The risk-factor name and optional rounding increment are taken from
\code{segments}. The risk-factor column in \code{data} must be numeric and contain only
finite, non-missing values. Values outside the original segmentation range
produce an error because their tariff treatment has not been supported by
the fitted GAM. The resulting factor can be used in a GLM or retained as a
candidate grouping for further actuarial review.
}
\examples{
\dontrun{
age_segments <- risk_factor_gam(
  MTPL,
  risk_factor = "age_policyholder",
  claim_count = "nclaims",
  exposure = "exposure"
) |>
  derive_tariff_segments()

MTPL |>
  add_tariff_segments(age_segments, name = "age_policyholder_segment")
}

}
\seealso{
\code{\link[=risk_factor_gam]{risk_factor_gam()}}, \code{\link[=derive_tariff_segments]{derive_tariff_segments()}}
}
\author{
Martin Haringa
}
