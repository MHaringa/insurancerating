% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_refinement.R
\name{relativities}
\alias{relativities}
\title{Combine sublevel splits into a relativity specification}
\usage{
relativities(...)
}
\arguments{
\item{...}{One or more objects created by \code{\link[=split_level]{split_level()}}.}
}
\value{
A named list of data frames suitable for the \code{relativities} argument
of \code{\link[=add_relativities]{add_relativities()}}.
}
\description{
Combine one or more definitions created by \code{\link[=split_level]{split_level()}} into the named
relativity specification expected by \code{\link[=add_relativities]{add_relativities()}}.
}
\details{
Each input represents one existing level of \code{model_variable} and the detailed
\code{split_variable} levels that replace it. Parent levels must be unique within
the combined specification. Levels of the original model variable that are
not included remain unsplit.

\code{relativities()} only assembles and validates the specification. It does not
estimate, normalise or apply the supplied relativities. Exposure
normalisation, when requested, is performed by \code{\link[=add_relativities]{add_relativities()}}.
}
\examples{
relativities(
  split_level(
    "residential",
    new_levels = c("flat", "house"),
    relativities = c(0.95, 1.05)
  ),
  split_level(
    "commercial",
    new_levels = c("shop", "office"),
    relativities = c(1.10, 0.90)
  )
)

}
\seealso{
\code{\link[=split_level]{split_level()}}, \code{\link[=add_relativities]{add_relativities()}}
}
\author{
Martin Haringa
}
