% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rating_table.R
\name{rating_table}
\alias{rating_table}
\title{Present fitted pricing-model effects as a rating table}
\usage{
rating_table(
  ...,
  model_data = NULL,
  exposure = TRUE,
  exposure_output = NULL,
  exponentiate = TRUE,
  significance = FALSE,
  reference_first = TRUE,
  level_order = c("estimate_descending", "estimate_ascending", "model", "alphabetical"),
  level_order_by_risk_factor = NULL,
  numeric_level_order = c("ascending", "as_specified"),
  risk_factor_order = c("model", "alphabetical"),
  order_model = NULL,
  round_exposure = 0,
  exposure_name = NULL,
  signif_stars = NULL
)
}
\arguments{
\item{...}{One or more fitted \code{glm} objects, including models returned by
\code{\link[=refit]{refit()}}. Object expressions are used to construct the dynamic estimate
column names.}

\item{model_data}{Optional data frame used to fit the models. If \code{NULL},
the function tries to use \code{model$data} for each supplied model.}

\item{exposure}{Logical or character string. If \code{TRUE}, exposure is added
if it can be inferred from the model. If \code{FALSE}, no exposure is added.
If a character string is supplied, it is interpreted as the exposure column
name.}

\item{exposure_output}{Optional character string naming the exposure column
in the output.
If \code{NULL}, the original exposure column name is used.}

\item{exponentiate}{Logical. If \code{TRUE}, coefficients are
exponentiated and shown as relativities. If \code{FALSE}, coefficients are shown
on the model scale.}

\item{significance}{Logical. If \code{TRUE}, add a separate \verb{signif_*} column for
each model containing significance indicators based on coefficient
p-values. The corresponding \verb{est_*} columns remain numeric.}

\item{reference_first}{Logical. If \code{TRUE}, place the reference level first
when the global ordering of a nominal risk factor is \code{"model"} or
\code{"alphabetical"}. Numeric levels, ordered factors, estimate-based ordering
and explicit per-factor overrides retain their selected order. For an
ordinary GLM, the reference is obtained from the fitted factor contrasts.
After \code{\link[=add_rebasing]{add_rebasing()}}, the selected rebasing level is used.}

\item{level_order}{Character string controlling the default order of nominal
factor levels. \code{"estimate_descending"} (default) places the highest fitted
effect first, \code{"estimate_ascending"} places the lowest first,
\code{"model"} retains the fitted model order and \code{"alphabetical"} sorts labels.
Numeric levels and explicitly ordered factors use their substantive order
instead.}

\item{level_order_by_risk_factor}{Optional named character vector providing
an ordering override for individual risk factors. Names identify risk
factors and values must be \code{"model"}, \code{"alphabetical"},
\code{"estimate_ascending"} or \code{"estimate_descending"}. For example,
\code{c(urbanisation = "model", sector = "estimate_descending")} preserves an
ordinal urbanisation scale while ordering sector relativities from high to
low. Numeric ordering still takes precedence.}

\item{numeric_level_order}{Character string controlling levels that are all
recognisable as numbers or numeric intervals. \code{"ascending"} orders them by
numeric value, or by the lower and then upper interval boundary, regardless
of \code{level_order}. This correctly orders labels such as \verb{(100,200]} and
\verb{(1000,2000]}. \code{"as_specified"} leaves these levels to \code{level_order}.
Numeric ordering takes precedence over \code{reference_first}, so the reference
level is not moved away from its numerical position.}

\item{risk_factor_order}{Character string controlling risk-factor order.
\code{"model"} retains the order in the fitted model; \code{"alphabetical"} sorts
risk-factor names. The intercept, when present, remains first.}

\item{order_model}{Optional character string naming the supplied model whose
level order, reference levels and estimates are used for sorting. This is
mainly relevant when several models are compared. If \code{NULL}, the first
supplied model is used. Both \code{"frequency"} and \code{"est_frequency"} are
accepted for a model object named \code{frequency}. If that model does not
contain a particular risk factor, the first supplied model containing the
factor provides its order and reference level.}

\item{round_exposure}{Non-negative number of digits used to round exposure.}

\item{exposure_name}{Deprecated. Use \code{exposure_output} instead.}

\item{signif_stars}{Deprecated. Use \code{significance} instead.}
}
\value{
A data frame with classes \code{"rating_table"}, legacy \code{"riskfactor"}
and \code{"data.frame"}. It can be inspected and manipulated directly with
ordinary data-frame operations. For backward compatibility, \code{x$df} returns
the same table without the package-specific class and metadata. The table
contains:
\describe{
\item{risk_factor}{Model term or risk-factor name.}
\item{level}{Factor level or term representation.}
\item{\verb{est_*}}{Coefficient or exponentiated relativity for each supplied
model. The suffix is derived from the model expression.}
\item{\verb{signif_*}}{Optional significance indicator for each model.}
\item{Exposure column}{Optional aggregated exposure, retaining the
requested output name.}
}
}
\description{
Extract coefficients from one or more fitted GLMs and organise them by risk
factor and level. Reference levels are made explicit, coefficients can be
expressed as multiplicative relativities, and portfolio exposure can be
attached to support actuarial review.
}
\details{
\subsection{Coefficients and relativities}{

The table contains one row per model term level. For factor variables,
the reference level is added explicitly with relativity \code{1} when
\code{exponentiate = TRUE}, or coefficient \code{0} when \code{exponentiate = FALSE}.
Numeric model terms are retained on the scale supplied by the fitted model
structure.

Estimate columns are named from the supplied model expressions, for example
\code{est_frequency} for an object named \code{frequency}. When several models are
supplied, their effects are joined by risk factor and level.
}

\subsection{Actuarial interpretation}{

With a log-link GLM, exponentiated coefficients represent conditional
multiplicative effects relative to the model reference level. They should be
interpreted together with the model specification and should not be confused
with the unadjusted observed measures returned by \code{\link[=factor_analysis]{factor_analysis()}}.

Exposure by level provides context for the amount of portfolio information
supporting each fitted effect. Significance indicators describe evidence
conditional on the fitted model; they do not measure practical materiality,
temporal stability or suitability for direct tariff implementation.

Comparing multiple models is useful for assessing changes between
unrestricted and refined specifications, or between alternative model
formulations. Comparable response definitions and coefficient scales remain
the responsibility of the analyst.
}

\subsection{Row order and reference levels}{

By default, risk factors follow the model formula. Numeric levels and
intervals are shown from low to high, explicitly ordered factors retain their
factor-level sequence, and remaining nominal factors are shown from highest
to lowest fitted effect. This separates structural order from an ordering
used to compare tariff differentiation.

\code{reference_first} applies only when a nominal factor uses model or
alphabetical order. It does not move the reference level ahead of a numeric,
ordinal or estimate-based sequence. The reference remains recorded in the
rating-table metadata, including a reference selected with \code{\link[=add_rebasing]{add_rebasing()}}.

Alternative level ordering is useful for specific review tasks. Alphabetical
order supports lookup and export, while model order can retain a deliberately
specified factor sequence. Use \code{level_order_by_risk_factor} when nominal and
ordinal factors require different treatment in the same table. With several
models, \code{order_model} defines which fitted specification provides
estimate-based ordering. \code{\link[=as_gt]{as_gt()}} and \code{\link[=autoplot.rating_table]{autoplot.rating_table()}} retain the
row order established here.

Only a factor stored with \code{ordered = TRUE} is identified automatically as an
ordinal scale. A regular factor may also have deliberately arranged levels,
but that intention cannot be distinguished reliably from an arbitrary model
order. Use \code{level_order_by_risk_factor = c(variable = "model")} to preserve
that sequence explicitly.

Numeric labels and intervals receive separate treatment because alphabetical
ordering can give an incorrect tariff sequence. With the default
\code{numeric_level_order = "ascending"}, a risk factor is sorted numerically only
when every displayed level is either a complete number or a valid interval
with two numeric boundaries. Mixed labels such as \code{"Industry 1"} remain
categorical. Set \code{numeric_level_order = "as_specified"} when the fitted model
order or another \code{level_order} should be retained deliberately.
}

\subsection{Significance indicators}{

When \code{significance = TRUE}, every model receives its own \verb{signif_*} column.
For example, models named \code{frequency} and \code{severity} produce
\code{est_frequency}, \code{signif_frequency}, \code{est_severity} and
\code{signif_severity}. Keeping estimates and indicators separate preserves the
numeric type of the fitted effects for subsequent calculations, filtering
and export.

\code{\link[=as_gt]{as_gt()}} combines each estimate with its corresponding significance
indicator for presentation and adds the significance thresholds as a source
note below the table. Reference levels generally have no separate
coefficient test and therefore have no significance indicator.

\code{rating_table()} accepts fitted models only. A \code{rating_refinement}
specification must first be fitted with \code{\link[=refit]{refit()}}.
}
}
\examples{
df <- MTPL
df$zip <- as.factor(df$zip)

freq <- glm(
  nclaims ~ bm + zip + offset(log(exposure)),
  family = poisson(),
  data = df
)

fitted_effects <- rating_table(
  freq,
  model_data = df,
  exposure = "exposure"
)

fitted_effects
head(fitted_effects)

# The historical accessor remains available for existing code
identical(fitted_effects$df, as.data.frame(fitted_effects))
if (requireNamespace("gt", quietly = TRUE)) {
  as_gt(fitted_effects)
}

# Keep coefficients on the model scale instead of exponentiating
rating_table(
  freq,
  model_data = df,
  exposure = "exposure",
  exponentiate = FALSE
)

# Significance is supplementary to exposure and stability assessment
rating_table(
  freq,
  model_data = df,
  exposure = "exposure",
  significance = TRUE
)

# Compare two fitted models side by side
freq_simple <- glm(
  nclaims ~ bm + offset(log(exposure)),
  family = poisson(),
  data = df
)

rating_table(
  freq_simple,
  freq,
  model_data = df,
  exposure = FALSE
)

# Order all levels by fitted relativity
rating_table(
  freq,
  model_data = df,
  exposure = "exposure",
  level_order = "estimate_descending"
)

}
\seealso{
\code{\link[=as_gt]{as_gt()}} for grouped tabular presentation,
\code{\link[=autoplot.rating_table]{autoplot.rating_table()}} for graphical comparison,
\code{\link[=factor_analysis]{factor_analysis()}} for observed portfolio experience, and \code{\link[=refit]{refit()}} for
fitting a refinement specification.
}
\author{
Martin Haringa
}
