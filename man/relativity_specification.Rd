% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_refinement.R
\name{relativity_specification}
\alias{relativity_specification}
\alias{split_level}
\alias{relativities}
\title{Define sublevel relativity specifications}
\usage{
split_level(level, new_levels, relativities)

relativities(...)
}
\arguments{
\item{level}{Character string. Existing level of the risk factor to split.}

\item{new_levels}{Character vector. Levels of the more detailed portfolio
variable within \code{level}.}

\item{relativities}{Numeric vector. Multiplicative relativities corresponding
to \code{new_levels}. Must have the same length as \code{new_levels}.}

\item{...}{One or more objects created by \code{split_level()}.}
}
\value{
\code{split_level()} returns a named list of length one. Its name is
\code{level}; its value is a data frame with columns \code{new_level} and
\code{relativity}. \code{relativities()} returns the combined named list expected by
the \code{relativities} argument of \code{\link[=add_relativities]{add_relativities()}}.
}
\description{
Use \code{split_level()} to describe how one existing GLM factor level is divided
into more detailed portfolio levels with specified multiplicative
relativities. Use \code{relativities()} to combine one or more of these definitions
into the specification supplied to \code{\link[=add_relativities]{add_relativities()}}.
}
\details{
\code{level} identifies the existing parent level in \code{model_variable}.
\code{new_levels} identifies the corresponding levels of \code{split_variable}.
\code{relativities} gives their relative tariff effects before any optional
exposure normalisation by \code{\link[=add_relativities]{add_relativities()}}.

Each call to \code{split_level()} represents one parent level. Several parent
levels can be refined in one step by passing their definitions to
\code{relativities()}. Parent levels must be unique within the combined
specification. Levels of the original model variable that are not included
remain unsplit.

These helpers assemble and validate explicit tariff assumptions. They do not
estimate, normalise or apply the supplied relativities and do not alter a
fitted GLM. Exposure normalisation, when requested, is performed by
\code{\link[=add_relativities]{add_relativities()}}.
}
\examples{
construction_split <- split_level(
  level = "residential",
  new_levels = c("flat", "house"),
  relativities = c(0.95, 1.05)
)

relativities(
  construction_split,
  split_level(
    "commercial",
    new_levels = c("shop", "office"),
    relativities = c(1.10, 0.90)
  )
)

}
\seealso{
\code{\link[=add_relativities]{add_relativities()}}
}
\author{
Martin Haringa
}
