% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate.R
\name{autoplot.factor_analysis}
\alias{autoplot.factor_analysis}
\title{Plot observed portfolio experience by risk factor}
\usage{
\method{autoplot}{factor_analysis}(
  object,
  metrics = NULL,
  ncol = 1,
  show_exposure = TRUE,
  show_exposure_labels = TRUE,
  sort_by_exposure = FALSE,
  level_order = NULL,
  decimal_mark = ",",
  line_color = NULL,
  bar_fill = NULL,
  label_width = 50,
  flip_bars = FALSE,
  show_total = FALSE,
  total_color = NULL,
  total_name = NULL,
  rotate_angle = NULL,
  custom_theme = NULL,
  remove_underscores = FALSE,
  compact_x_axis = TRUE,
  show_plots = NULL,
  background = NULL,
  labels = NULL,
  sort = NULL,
  sort_manual = NULL,
  dec.mark = NULL,
  color = NULL,
  color_bg = NULL,
  coord_flip = NULL,
  remove_x_elements = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{factor_analysis} or \code{univariate} object produced by
\code{\link[=factor_analysis]{factor_analysis()}} or \code{\link[=univariate]{univariate()}}.}

\item{metrics}{Numeric or character vector specifying which metrics to plot
(default is all available metrics). The numeric positions are:
\itemize{
\item 1. Frequency (\code{nclaims / exposure})
\item 2. Average severity (\code{severity / nclaims})
\item 3. Risk premium (\code{severity / exposure})
\item 4. Loss ratio (\code{severity / premium})
\item 5. Average premium (\code{premium / exposure})
\item 6. Exposure
\item 7. Severity
\item 8. Number of claims
\item 9. Premium
}
Character values can be \code{"frequency"}, \code{"average_severity"},
\code{"risk_premium"}, \code{"loss_ratio"}, \code{"average_premium"}, \code{"exposure"},
\code{"claim_amount"}, \code{"claim_count"}, and \code{"premium"}.}

\item{ncol}{Positive whole number. Number of columns in the plot composition.}

\item{show_exposure}{Show exposure as background bars behind line plots
(default = TRUE).}

\item{show_exposure_labels}{Show labels with the exposure bars
(default = TRUE).}

\item{sort_by_exposure}{Sort risk factor levels into descending order by
exposure (default = FALSE).}

\item{level_order}{Custom order for risk factor levels; character vector
(default = NULL).}

\item{decimal_mark}{Decimal mark; defaults to \code{","}.}

\item{line_color}{Optional override for line/point color.
If NULL (default), colors are taken from the internal palette.
If specified, the chosen color is applied to all line-based plots.}

\item{bar_fill}{Optional override for background bar color.
If NULL (default), the background color is taken from the internal palette.
If specified, the chosen color is applied to all background bars.}

\item{label_width}{Width of labels on the x-axis (default = 10).}

\item{flip_bars}{Logical. If \code{TRUE}, flip cartesian coordinates for bar plots
(metrics 6 to 9). This option does not affect the line-based plots for
metrics 1 to 5.}

\item{show_total}{Show line for total if \code{by} is used (default = FALSE).}

\item{total_color}{Color for total line (default = \code{"black"}).}

\item{total_name}{Legend name for total line (default = NULL).}

\item{rotate_angle}{Numeric value for angle of labels on the x-axis (degrees).}

\item{custom_theme}{List with customized theme options.}

\item{remove_underscores}{Logical; remove underscores from labels
(default = FALSE).}

\item{compact_x_axis}{Logical. When \code{TRUE} and \code{ncol == 1},
x-axis components are removed from all plots except the last one.
The following elements are suppressed:
\itemize{
\item \code{axis.title.x}
\item \code{axis.text.x}
\item \code{axis.ticks.x}
}
This prevents duplicated x-axes in vertically stacked patchwork plots.
Defaults to \code{TRUE}.}

\item{show_plots}{Deprecated. Use \code{metrics} instead.}

\item{background}{Deprecated alias for \code{show_exposure}.}

\item{labels}{Deprecated alias for \code{show_exposure_labels}.}

\item{sort}{Deprecated alias for \code{sort_by_exposure}.}

\item{sort_manual}{Deprecated alias for \code{level_order}.}

\item{dec.mark}{Deprecated alias for \code{decimal_mark}.}

\item{color}{Deprecated alias for \code{line_color}.}

\item{color_bg}{Deprecated alias for \code{bar_fill}.}

\item{coord_flip}{Deprecated alias for \code{flip_bars}.}

\item{remove_x_elements}{Deprecated alias for \code{compact_x_axis}.}

\item{...}{Currently unused.}
}
\value{
A \code{patchwork} composition containing the requested ggplot panels.
}
\description{
Visualise the exposure and observed actuarial metrics calculated by
\code{\link[=factor_analysis]{factor_analysis()}}. The plots support assessment of portfolio composition,
observed frequency, severity, risk premium and loss ratio across risk-factor
levels.

The observed patterns are descriptive. They do not adjust for correlations
with other risk factors and should therefore not be interpreted as
multivariate tariff relativities.
}
\details{
For rate-based metrics, exposure can be shown as background bars so that
observed level differences can be interpreted together with portfolio
volume. Levels with little exposure or few claims can produce volatile
observed metrics and should be assessed accordingly.

When the factor analysis contains one or more \code{by} variables, separate
observed series are shown. \code{show_total = TRUE} adds the aggregate portfolio
series for comparison. Sorting and manual level ordering affect only the
presentation; the underlying summaries are unchanged.
}
\examples{
# Plot observed frequency and risk premium
x <- factor_analysis(MTPL2,
                     x = "area",
                     severity = "amount",
                     nclaims = "nclaims",
                     exposure = "exposure")
autoplot(x, metrics = c("frequency", "risk_premium"))

}
\seealso{
\code{\link[=factor_analysis]{factor_analysis()}}, \code{\link[=add_portfolio_experience]{add_portfolio_experience()}}
}
\author{
Marc Haine, Martin Haringa
}
