% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{set_reference_level}
\alias{set_reference_level}
\title{Set the reference level of a factor}
\usage{
set_reference_level(
  x,
  weight = NULL,
  method = "largest_weight",
  reference_level = NULL
)
}
\arguments{
\item{x}{A factor (unordered). Character vectors should be converted to
factor before use.}

\item{weight}{A numeric vector of the same length as \code{x},
typically representing exposure or frequency weights. Required when
\code{method = "largest_weight"}.}

\item{method}{Character. Method used to choose the reference level.
Supported methods are \code{"largest_weight"} and \code{"manual"}.}

\item{reference_level}{Character string with the level to use as reference
when \code{method = "manual"}.}
}
\value{
A factor of the same length as \code{x}, with the selected reference
level set as the first level.
}
\description{
Relevels a factor so that the selected category becomes the reference
(first) level. By default, the reference level is chosen as the level with
the largest total weight, for example the largest exposure in an insurance
portfolio. Use \code{method = "manual"} with \code{reference_level} when a specific
business category should be the reference level.
}
\examples{
\dontrun{
library(dplyr)
df <- chickwts |>
mutate(across(where(is.character), as.factor)) |>
mutate(across(where(is.factor), ~set_reference_level(., weight)))

set_reference_level(df$feed, method = "manual", reference_level = "casein")
}

}
\references{
Kaas, Rob & Goovaerts, Marc & Dhaene, Jan & Denuit, Michel.
(2008). Modern Actuarial Risk Theory: Using R.
\doi{10.1007/978-3-540-70998-5}
}
\author{
Martin Haringa
}
