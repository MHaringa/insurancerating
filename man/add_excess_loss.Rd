% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excess_loss.R
\name{add_excess_loss}
\alias{add_excess_loss}
\title{Add calculated excess-loss columns to data}
\usage{
add_excess_loss(
  data,
  x,
  name = "excess_loss",
  include = c("amount", "share", "factor"),
  overwrite = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{x}{An \code{"excess_loss_vector"} returned by \code{\link[=calculate_excess_loss]{calculate_excess_loss()}}.}

\item{name}{Character string. Base output column name.}

\item{include}{Character vector with any of \code{"amount"}, \code{"share"},
\code{"factor"} and \code{"base"}. \code{"amount"} refers to the allocated excess-loss
amount, not the original claim amount column.}

\item{overwrite}{Logical. If \code{FALSE}, existing output columns cause an error.}
}
\value{
A \code{data.frame} with added columns.
}
\description{
\code{add_excess_loss()} adds output from \code{\link[=calculate_excess_loss]{calculate_excess_loss()}} to a
\code{data.frame}. It does not calculate anything itself and only copies stored
vectors from the attributes of \code{x}. This keeps the workflow auditable: the
excess-loss calculation is done once in \code{\link[=calculate_excess_loss]{calculate_excess_loss()}}, while
\code{add_excess_loss()} is only a data-preparation step for modelling, reporting
or later tariff refinement.

With the default \code{include = c("amount", "share", "factor")}, the function
adds the allocated excess-loss amount, the row-level share of the total
excess-loss amount and the allocation factor used to distribute the excess.
Use \code{include = "base"} when you also want to inspect the allocation base
(\code{allocation_weights * allocation_factor}).
}
\examples{
claims <- data.frame(
  segment = rep(c("A", "B"), each = 4),
  claim_amount = c(1000, 120000, 30000, 8000, 2000, 150000, 40000, 6000),
  exposure = rep(1, 8)
)
x <- calculate_excess_loss(
  claims,
  claim_amount = "claim_amount",
  exposure = "exposure",
  fit_threshold = 20000,
  excess_threshold = 100000,
  method = "empirical",
  allocation_method = "exposure",
  allocation_by = "segment",
  allocation_levels = c("A", "B")
)
add_excess_loss(claims, x, name = "large_loss")

}
\author{
Martin Haringa
}
